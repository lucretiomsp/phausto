name: Run Tests

on:
  push:
    paths-ignore:
      - README.md
  pull_request:
    paths-ignore:
      - README.md
  workflow_dispatch:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    name: ${{ matrix.smalltalk }} on ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-22.04
          - macos-latest
          - windows-latest
        smalltalk: [Pharo64-12]
        ston: [.smalltalk.ston]

    steps:
      # ------------------------------
      # Checkout
      # ------------------------------
      - name: Checkout repository
        uses: actions/checkout@v3

      # ------------------------------
      # Setup SmalltalkCI
      # ------------------------------
      - name: Setup SmalltalkCI
        uses: hpi-swa/setup-smalltalkCI@v1
        with:
          smalltalk-image: ${{ matrix.smalltalk }}

      # ------------------------------
      # Ubuntu: install system dependencies
      # ------------------------------
      - name: Install system libraries (Ubuntu)
        if: matrix.os == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y pulseaudio libpulse0 libjack0 unzip

      # ------------------------------
      # Prepare librariesBundle directory
      # ------------------------------
      - name: Prepare librariesBundle directory
        run: |
          mkdir -p "$HOME/.smalltalkCI/_builds/librariesBundle"

      # ------------------------------
      # Ubuntu: libraries bundle
      # ------------------------------
      - name: Install Libraries Bundle (Ubuntu)
        if: matrix.os == 'ubuntu-22.04'
        run: |
          unzip -q librariesBundle_Linux.zip \
            -d "$HOME/.smalltalkCI/_builds/librariesBundle"

      # ------------------------------
      # macOS: libraries bundle
      # ------------------------------
      - name: Install Libraries Bundle (macOS)
        if: matrix.os == 'macos-latest'
        run: |
          hdiutil attach phaustoLibrariesM1.dmg -mountpoint /Volumes/phausto -nobrowse -readonly
          cp -R /Volumes/phausto/* "$HOME/.smalltalkCI/_builds/librariesBundle"
          hdiutil detach /Volumes/phausto

      # ------------------------------
      # Windows: libraries bundle
      # ------------------------------
      - name: Install Libraries Bundle (Windows)
        if: matrix.os == 'windows-latest'
        shell: powershell
        run: |
          $dest = "$env:USERPROFILE\.smalltalkCI\_builds\librariesBundle"
          New-Item -ItemType Directory -Force -Path $dest | Out-Null
          Expand-Archive librariesBundle_windows_intel.zip $dest -Force

      # ------------------------------
      # Windows: DLL verification only
      # ------------------------------
      - name: Verify DLL and dependencies (Windows)
        if: matrix.os == 'windows-latest'
        shell: powershell
        run: |
          $dll = "$env:USERPROFILE\.smalltalkCI\_builds\librariesBundle\librariesBundle\libdynamic-engine.dll"

          if (-not (Test-Path $dll)) {
            Write-Error "libdynamic-engine.dll not found"
            exit 1
          }

          Write-Host "libdynamic-engine.dll found"
          $folder = Split-Path $dll
          Write-Host "DLLs in $folder:"
          Get-ChildItem "$folder\*.dll" | ForEach-Object { Write-Host $_.Name }

          # Basic load test (fails if dependent DLLs are missing)
          try {
              Add-Type -Path $dll
              Write-Host "DLL loaded successfully (basic dependency check)"
          } catch {
              Write-Error "Failed to load DLL. Likely missing dependencies."
              exit 1
          }

      # ------------------------------
      # Linux/macOS: set library environment
      # ------------------------------
      - name: Set library environment
        if: matrix.os != 'windows-latest'
        run: |
          echo "FAUST_LIB_PATH=$HOME/.smalltalkCI/_builds/librariesBundle/faustLibs" >> "$GITHUB_ENV"
          echo "LD_LIBRARY_PATH=$HOME/.smalltalkCI/_builds/librariesBundle" >> "$GITHUB_ENV"
          if [ "${{ matrix.os }}" = "macos-latest" ]; then
            echo "DYLD_LIBRARY_PATH=$HOME/.smalltalkCI/_builds/librariesBundle" >> "$GITHUB_ENV"
          fi

      # ------------------------------
      # Linux/macOS: run SmalltalkCI
      # ------------------------------
      - name: Run SmalltalkCI
        if: matrix.os != 'windows-latest'
        timeout-minutes: 20
        run: |
          smalltalkci -s ${{ matrix.smalltalk }} ${{ matrix.ston }}

      # ------------------------------
      # Windows: skip SmalltalkCI execution
      # ------------------------------
      - name: Skip Windows execution
        if: matrix.os == 'windows-latest'
        run: |
          echo "Windows execution skipped (requires ASIO backend)"
